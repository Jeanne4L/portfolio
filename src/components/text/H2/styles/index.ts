import newStyled from "@emotion/styled"

import { FontSizes, FontWeights } from "../../../../constants/fonts"

export const Title = newStyled.h2`
  font-size: ${FontSizes.H2};
  font-weight: ${FontWeights.BOLD};
`